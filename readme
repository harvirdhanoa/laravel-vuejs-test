Project Installation Steps

1. Clone git Repo

2. create .env file by copying .env.example and set database credentials

3. open terminal and run below commands

composer install

php artisan migrate

php artisan passport:install

npm install
